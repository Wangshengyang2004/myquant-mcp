[pytest]
addopts = -p no:cacheprovider --cov=server.api --cov=server.app --cov=server.config --cov=server.log_config --cov=server.mcp_server --cov-report=term-missing
norecursedirs = pytest-cache-files-* tmp*
markers =
    integration: tests that require a live GM/MyQuant environment
